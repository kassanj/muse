= simple_form_for @post do |f|
	
	%p= f.input :image, label: false

	%p= f.input :title, label: false, placeholder: "Title" 

	%p= f.input :link, label: false, placeholder: "Website"

	%p= f.input :description, label: false, placeholder: "Description"

	%p= f.button :submit